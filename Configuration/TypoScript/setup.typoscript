# Registration for the frontend
plugin.tx_form {
  settings {
    yamlConfigurations.1665729138 = EXT:form_pincodes/Configuration/Form/FormPincodesFrontend.yaml
#    formDefinitionOverrides {
# enable checkbox field if pincode codes are available
#      testPincodes {
#        renderables {
# first page of form
#          0 {
#            renderables {
# number of element in form
#              2 {
# another level (because of "Grid: Row")
#                renderingOptions {
#                  enabled = TEXT
#                  enabled.value = 0
#                  enabled.stdWrap {
#                    override {
#                      cObject = CONTENT
#                      cObject {
#                        table = tx_formpincodes_domain_model_pincodes
#                        select {
#                          pidInList = 51
#                          where = is_used=0
#                          max = 1
#                        }
#                        renderObj = TEXT
#                        renderObj.value = 1
#                      }
#                    }
#                  }
#                }
#              }
#            }
#          }
#        }
#      }
#    }
  }
}
